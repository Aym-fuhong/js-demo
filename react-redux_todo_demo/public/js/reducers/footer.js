export default function reducer(state = {filterName: 'ALL'}, action){
    switch (action.type){
        // case 'FILTER':
        //     console.log(state, 'todolist reducer state');
        //
        //     return [...state];
    }
    return [...state];
}